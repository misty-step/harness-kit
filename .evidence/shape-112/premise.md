# Premise: Harness eval bench and stronger simplification routing

Date: 2026-06-16

User asks driving this shape:

1. Ponytail should probably run by default on `/refactor`, `/shape`, and
   similar workflows where overengineering risk is high.
2. Harness Kit should investigate top external skills further because the
   current scout proves only candidate shape, not that Harness Kit's own
   primitives are better.
3. `/groom` should encourage creation and management of a project vision that
   guides brainstorming, grooming, sequencing, and work.
4. Shape a new ticket for designing and running evals for the harness,
   particularly against raw runs and alternative primitives.

Live repo state before this shape:

- `dietrich-ponytail` is installed, but only `delete-first` is routed through
  several first-party skills.
- `harness-engineering/references/mode-eval.md` defines the current eval
  contract: task, transcript, outcome, graders; A/B in worktrees; blinded fresh
  grading; serious repeated evals may graduate to Daedalus.
- `scout-skills` can classify external skills, but the top-skill report is not
  an outcome eval.
