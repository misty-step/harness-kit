# Harness Kit Index
# Generated: 2026-06-13T17:53:02Z
# Do not edit manually. Run: harness-kit-checks generate-index --repo .

skills:
  - name: ci
    description: "Audit CI gates, strengthen weak coverage, then drive green. Harness Kit uses the Rust-owned local gate, not Dagger, as the canonical repo check: `cargo run --locked -p harness-kit-checks -- check --re"
    source: first-party
    tags: [--,--locked,--re,-p,as,audit,canonical,cargo,check,ci]
  - name: code-review
    description: "Dispatch-shaped code review: fan the diff out to fresh-context reviewers across diverse providers and model families, synthesize, fix blockers, re-review until clean. Use when: \"review this\", \"code re"
    source: first-party
    tags: [across,blockers,clean,code,diff,dispatch-shaped,diverse,families,fan,fix]
  - name: deliver
    description: "Take one ticket or idea from raw intent to merge-ready (or shipped, when asked): context-first, docs→tests→code, live QA, refactor at three altitudes, semantic commits, diverse-provider review, ad"
    source: first-party
    tags: [ad,altitudes,asked,at,code,commits,context-first,diverse-provider,docs,idea]
  - name: design
    description: "Artifact-backed interface critique and polish for hierarchy, typography, layout, density, IA, interaction feel, content, brand fit, and taste. Requires screenshot, URL, rendered artifact, or explicit "
    source: first-party
    tags: [artifact,artifact-backed,brand,content,critique,density,explicit,feel,fit,hierarchy]
  - name: diagnose
    description: "Investigate, audit, triage, and fix. Systematic debugging, incident lifecycle, domain auditing, and issue logging. Feedback-loop-first protocol: reproduce or replay before root cause, pattern analysis"
    source: first-party
    tags: [analysis,audit,auditing,before,cause,debugging,domain,feedback-loop-first,fix,incident]
  - name: groom
    description: "Always-on backlog grooming. Tidy, brainstorm, interrogate, investigate, research, and simplify in a single loop. Tidy is not a mode — it happens every time. Strategic-layer work is a mega-sweep: swa"
    source: first-party
    tags: [a,always-on,backlog,brainstorm,every,grooming,happens,in,interrogate,investigate]
  - name: harness-engineering
    description: "Harness engineering for Harness Kit primitives: skills, shared doctrine, provider roster, harness configs, gates, evals, bootstrap, and sync logic. Use for \"improve the harness\", \"harness engineering\""
    source: first-party
    tags: [bootstrap,configs,doctrine,engineering,evals,gates,harness,improve,kit,logic]
  - name: qa
    description: "Verify the running thing works. Browser walks for web, request replay for APIs, shell smoke for CLIs, consumer builds for libraries, tool-call replay for MCP. \"Tests pass\" is not QA. Use when: \"run QA"
    source: first-party
    tags: [apis,browser,builds,clis,consumer,is,libraries,mcp,pass,qa]
  - name: research
    description: "Web research, multi-AI delegation, and multi-perspective validation. /research [query], /research delegate [task], /research thinktank [topic]. Use when: \"search for\", \"look up\", \"research\", \"delegate"
    source: first-party
    tags: [delegate,delegation,look,multi-ai,multi-perspective,query,research,search,task,thinktank]
  - name: roster
    description: "Enumerates the peer AI agent CLIs installed on this machine (codex, claude, pi, opencode, cursor-agent, grok, agy, hermes, thinktank) and how to invoke each headlessly. A capability map, not a quota: "
    source: first-party
    tags: [a,agent,agy,ai,capability,claude,clis,codex,cursor-agent,enumerates]
  - name: shape
    description: "Shape a raw idea into something buildable. Product + technical exploration. Spec, design, critique, plan. Output is a context packet. Use when: \"shape this\", \"write a spec\", \"design this feature\", \"pl"
    source: first-party
    tags: [a,buildable,context,critique,design,exploration,feature,idea,is,output]
  - name: sprites
    description: "Run lane cards on Fly Sprites: remote, isolated, scale-to-zero sandboxes for heavy or parallel agent work. Golden-checkpoint provisioning so lanes start on a ready sprite with zero setup tokens. Use w"
    source: first-party
    tags: [a,agent,cards,fly,golden-checkpoint,heavy,isolated,lane,lanes,on]

agents:
  []

# Collections are defined in collections.yaml
