# Premise Source Grader

Run the Rust grader from the Harness Kit source root:

```sh
cargo run --locked -p harness-kit-checks -- premise-source validate <candidate-packet>
```

Run the fixture self-test with:

```sh
cargo run --locked -p harness-kit-checks -- premise-source self-test
```
