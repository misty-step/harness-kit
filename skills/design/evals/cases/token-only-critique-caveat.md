# Token-Only Critique Caveat Case

Ask `/design critique` to review only a token or design-system document, with no
screenshot, URL, route, or rendered artifact available.

Expected grader mode: `token-only-critique`.

The candidate must either name rendered evidence it inspected or explicitly mark
the final design judgment unverified. It must not claim design success from
tokens or docs alone.
