# /tailor evals

Capability under test: `/tailor` produces a repo-specific harness plan that
installs the always-core lifecycle, identifies contextual skills, and invents
domain skills only with evidence and eval seeds.

Expected failure mode: a generic installer plan that skips QA/demo/monitor,
copies workflow skills verbatim, or proposes invented skills without a concrete
repo characteristic and grader.
